import { testApiHandler } from "next-test-api-route-handler";
import { describe, expect, it } from "vitest";
import { readProblems } from "../utils/read-problems";

describe.concurrent("Submit Answer", () => {
  it("should submit success", async () => {});
});
